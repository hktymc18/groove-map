# GROOVE MAP ゲーム化ロードマップ（画面設計・実装計画）

対象: 20〜30代・エネルギーはあるが管理は続かない層。
方針: **「営業管理ツール」から「毎日3分の習慣ゲーム」へ。**

## 設計原則（全Phaseに適用）
1. **罰を与えない** — 未達の赤字表示・最下位晒しは禁止。ネガティブ情報は本人とリーダーのみ。
2. **入力＝報酬の瞬間** — 記録した瞬間に必ず何かが返る（XP・バー・光る）。
3. **1日の遊び方を1画面に** — 毎日使うのはホームだけ。他タブは「たまに見る場所」。
4. ゲーミフィケーションは**プロフィールからOFF可**（真面目派向け）。

## コアループ
```
朝: ホーム→今日のクエスト3つ（自動生成）
昼: クイック記録ワンタップ（+CT等）→ 即XP/バー
夜: 「今日を締める」ワンタップ日報 → ストリーク更新 → チームフィードに流れる
週: 月曜「先週の成長レポート」/ 日曜「週次ふりかえり3問」
月: 翌月コピー→リザルト画面→シェア画像→新しい月スタート
```

---

## Phase A: ホーム再設計＋ワンタップ記録（土台・最優先）

### A-1 新ホーム「今日」
```
┌──────────────────────────┐
│ 🔥72日連続   Lv.12 ▓▓▓▓░ 2,340XP │ ← 既存 state.game
│ 保険残り1回（週1回休んでもOK）        │
│──────────────────────────│
│ 今日のクエスト (1/3)                │
│ ✅ CT取りを2件        +20xp        │
│ ⬜ 田中さんをフォロー(14日停滞) [GO]  │
│ ⬜ OL記録を1件         [GO]        │
│──────────────────────────│
│ クイック記録（押すだけ）              │
│ [＋アクション3][＋CT取り2][＋CT1]    │
│ [＋FT0][＋OL記録0][＋研修0]        │
│──────────────────────────│
│ 🌙 今日を締める（21時〜）  [締める]   │
└──────────────────────────┘
```
- **クエスト自動生成 `buildDailyQuests()`**（毎朝、優先度順に3つ）:
  1. GOALの日次目標から不足分（`goals.plan.funnel` × 当日残）
  2. 停滞アラート（既存 `ckLatestStepDate` / OL 14日無記録）→「◯◯さんをフォロー」
  3. 研修 `nextDate` が今日/明日 →「◯◯さんの研修に同席」
- **クイック記録 `quickRecord(stageKey)`**: `goals.plan.dailyActuals[today][stage]++` → 保存 → +1フロート演出＋XPトースト。
  - ⚠️ **データ統一**: 実績の真実は `dailyActuals` に一本化。GOALフォーカスの月次ステッパーは「差分を今日のdailyActualsに加算」する実装に変更し、`monthlyActuals` は読み取り互換のみ残して廃止（二重計上防止）。
- **夜の締め `closeToday()`**: 今日の合計を表示 → [保存] → streak++・+5XP・フィード投稿（Phase C以降）。
- XP経済（定数テーブル `GAME_XP` で一元管理・調整可能に）:
  `アクション+2 / CT取り+3 / CT+5 / FT+8 / OL+5 / 研修+8 / フロント+50 / クエスト各+10〜20 / 全クエスト+30 / 締め+5`

### A-2 ストリーク保険
- `state.game.streakInsurance`（週1回復）。日付が飛んでも保険があれば消費して継続。
- 表示: ホームに「保険残り◯回」。切れそうな21時に画面内バナー（push は Phase N）。

### A-3 おかえりミッション
- `lastActiveDate` が7日以上前なら、責めずに「おかえり！1クエストでストリーク半分復活」カード。

---

## Phase B: 月末リザルト＋シェア画像

### B-1 リザルト画面（翌月コピー時に全画面表示）
```
2026年7月 リザルト ／ 名前・屋号・タイトル
🏆 月間目標 達成！ フロント 2/2
[GSV 12,400 ↑][CT 34][決定率18%↑][記録27/31日]
今月の昇格: 川野 Q2→Q3 ／ 中西 LOI→Q2
獲得バッジ: 🔥30日連続 🎯月間達成 ＋未取得枠（？？？）
[画像でシェア][チームに投稿]
```
- `copyToNextMonth` の確定後に `showMonthlyResult(prevMonth)` を呼ぶ。データは既存 stats/dailyActuals/昇降格changesから集計。
- 未取得バッジを「？？？」で見せる＝コレクション欲。

### B-2 シェア画像生成 `generateResultImage()`
- Canvas 1080×1350（LINE/インスタ縦比率）に描画 → `canvas.toBlob` → `navigator.share`（iOS PWA対応）／非対応はダウンロード。
- 昇格時は個人の「昇格証明書」カードも同関数で（`gameCheckPromotion` から呼ぶ）。
- ⚠️ 演出はアプリ内、**画像はシンプルで格好良く**（外で見られるものなので）。

---

## Phase C: チームフィード＋称賛（要ルール変更）

### C-1 フィード画面（新ビュー「チーム」）
```
[チーム] BASE REVE
──────────────
🎉 川野さんが Q3 に昇格！     👏12 🔥3
🔥 山内さんが全クエスト達成     👏5
🏆 中西さんが 30日連続 達成    👏8 💪2
──────────────
（スタンプのみ・コメント無し＝ハードル最低）
```
- **データ**: `feeds/{unionKey}/items/{id}` = `{uid, name, type, text, ts}`、リアクションは `…/items/{id}/reactions/{uid}` = `{emoji}`。
- `unionKey` はプロフィール保存時に正規化して `users/{uid}.unionKey` に保持（ルールで文字列加工ができないため）。
- **自動投稿**: 昇格 / 全クエスト達成 / ストリーク7・30・100日 / 月間目標達成 / BR誕生。手動投稿は無し（荒れ防止・省力）。
- **ルール追加（要コンソール反映）**:
  ```
  match /feeds/{unionKey}/items/{id} {
    allow read:   if signedIn() && profile(request.auth.uid).get('unionKey','')==unionKey;
    allow create: if signedIn() && request.resource.data.uid==request.auth.uid
                  && profile(request.auth.uid).get('unionKey','')==unionKey;
    match /reactions/{ruid} { allow read: if signedIn(); allow write: if isSelf(ruid); }
  }
  ```
- 通知バッジ: フィード新着数をチームタブに。

### C-2 バディ機能
- `users/{uid}.buddyUid`。ホームに「相方: ◯◯さん 今日 2/3 達成」1行表示。相互承認制（申請→承諾）。

### C-3 週間MVP（リーダー）
- リーダー（admin）が週1回ワンタップ選出 → フィード投稿＋本人にバッジ。`feeds/{unionKey}/mvp/{week}`。

---

## Phase D: シーズン制（実イベント連動）

- GOALタイムラインの旗（`goals.plan.events`）をタップ → **シーズンパネル**:
```
✈️ RRR TRIP まで 62日
シーズン目標: フロント+4 ／ 現在 2/4 ▓▓░░
報酬トラック: [25% 🎖][50% 🎖][75% 🎖][100% ✈️限定バッジ]
```
- `events[].target`（フロント数等）を追加。進捗は期間中のdailyActuals合計。
- シーズン限定バッジは `state.game.badges` に `season:evId` 形式で。
- 月次リザルトにシーズン進捗行を追加。

---

## Phase E: マップの生命化＋週次成長レポート

### E-1 ノードの生死表現
- `memberActivityLevel(m)`: 最終活動日（olLog/traineeHistory/events の最新日付）から
  `active(7日)/idle(14日)/dormant(30日)` を判定。
- 表示: active＝枠が淡く脈打つ（CSS animation）／dormant＝彩度低下＋💤。
  PC SVGは `class` 付与、モバイル `.nc` は既存クラスに追加。**重くならないようCSSのみ・JS常駐なし**。
- タップで「最後の活動: 6/28 OL記録」を情報パネルに表示（既存 cardSingleTap に1行追加）。

### E-2 週次成長レポート（月曜の朝、ホーム最上部カード）
```
🌱 先週のあなたの組織
+1人 ／ GSV +800 ／ 昇格1 ／ フォロー要 3人 → [今日のクエストへ]
```
- クライアント側で先週分を集計（`weeklySnapshot` を meta に保存して差分）。

---

## Phase F: 信頼性（数字の信用＝アプリの寿命）

1. **GSV検算**: ノードの情報パネルに「内訳」→ `自分の分 + 各直下(名前:数値)` をリスト表示。`calcGSV` の途中経過を返す `explainGSV(mid)` を追加。
2. **変更履歴**: `autoSave` 時にメンバーの追加/削除/タイトル変更/GSV変更だけを `maps/{uid}/meta/history` に追記（最大200件・古い順に破棄）。表示はデータタブに「履歴」。
3. **共同編集のマージ改善**: 保存前に remote を再読取し、メンバー単位 `updatedAt` で新しい方を採用（last-write-wins をメンバー粒度に）。
4. **バージョン自動更新**: sw.js の `updatefound` → 「新しいバージョンがあります[更新]」トースト → reload。「アプリ再起動して」の案内を撲滅。

---

## Phase G: FTUE（最初の5分）

- 承認後の初回ログインで4ステップウィザード（スキップ可）:
  `①自分を配置 → ②メンバーを3人追加 → ③頂上を設定 → ④今日のクエスト表示`
- 各ステップ完了で紙吹雪＋XP。完了までホームに進捗チェックリスト残置。
- 空マップには「見本の組織」を半透明ゴースト表示（`buildNodeHTML` 流用・編集不可）。
- 実装: `state.ftue = {step:0..4}` を meta に保存。

---

## Phase H: 自動コーチング

- 月曜のレポートに**1行だけ**アドバイスを追加（ルールベースで開始、閾値は定数）:
  - 決定率↓ →「FTを今週+2」
  - CT取り目標比<50% →「アクション数を先に増やそう」
  - 停滞3人以上 →「新規より先にフォローDay推奨」
- 生成はクライアントJSのみ（AI連携はその後の拡張）。

---

## Phase N: プッシュ通知（横断・後段）

- FCM Web Push（iOS16.4+ PWA対応）。`firebase-messaging` + sw.js 改修 + APNs設定が必要で重め。
- それまでの代替: `navigator.setAppBadge`（アイコンバッジ）＋画面内バナー。
- 通知ポリシー: 「仲間の昇格🎉」「あなたのストリーク危機🔥」「アポ前日」のみ。**催促はしない**。

---

## 実装順序・目安

| 順 | Phase | 内容 | 規模 | ルール反映 |
|---|---|---|---|---|
| 1 | A | ホーム再設計・クイック記録・保険 | 大（2〜3リリース） | 不要 |
| 2 | B | リザルト＋シェア画像 | 中 | 不要 |
| 3 | C | フィード＋称賛＋バディ＋MVP | 大 | **必要** |
| 4 | D | シーズンパス | 小〜中 | 不要 |
| 5 | E | マップ生命化＋週次レポート | 中 | 不要 |
| 6 | F | 検算・履歴・マージ・自動更新 | 中 | 不要 |
| 7 | G | FTUE | 中 | 不要 |
| 8 | H | コーチング | 小 | 不要 |
| 9 | N | Push通知 | 大（FCM/APNs） | 別作業 |

## 計測（効果が出てるかの判定）
- 記録日数/月（`gameDays`）、7日継続率、クエスト完了率、フィード反応数、シェア画像生成数。
- まずは `state.game` に匿名カウンタを足すだけでよい（外部分析は不要）。

## 並行開発メモ
- 本書は Mac版Claude Code / web版Claude 共通の設計書。**着手前に必ず git fetch**。
- Phase C のルール変更はオーナーがFirebaseコンソールで反映。
